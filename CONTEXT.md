# URL 단축기

계정별로 URL을 단축하고 기록을 관리하는 서비스.

## Language

**단축 링크(ShortLink)**:
사용자가 원본 URL로부터 생성한 리다이렉트 경로. `{계정 식별자}/{도메인 축약어}/{별칭}` 형태로 구성된다. 계정 식별자가 경로에 포함되므로 `도메인 축약어 + 별칭` 조합은 계정별로만 유일하면 된다. 하나의 원본 URL에 여러 개의 단축 링크(별칭)가 동시에 존재할 수 있다 — 원본 URL과 단축 링크는 일대다 관계이며, 각 단축 링크는 독립적으로 삭제할 수 있다. 생성된 단축 링크의 원본 URL과 별칭은 수정할 수 없다(불변); 별칭을 바꾸고 싶으면 새 단축 링크를 추가하고 기존 것을 삭제한다. 각 단축 링크는 공개/비공개 상태를 가지며 기본값은 비공개다. 삭제(소프트 삭제)된 별칭은 같은 계정에서 재사용해 새 원본 URL에 다시 배정할 수 있다.

**대시보드(Dashboard)**:
로그인한 계정 소유자만 볼 수 있는 페이지. 본인이 만든 모든 단축 링크(공개+비공개)와 각각의 클릭 수를 목록으로 보여준다.

**프로필 페이지(Profile Page)**:
`{계정 핸들}` 경로로 접근하는 페이지. 해당 계정이 공개로 설정한 단축 링크만 보여주며, 누구나 열람 가능하다.

**계정 핸들(Account Handle)**:
가입 시 사용자가 직접 설정하는 고유 식별자. 단축 링크 경로의 첫 부분으로 쓰인다.
_Avoid_: 사용자 ID, 유저네임

**도메인 축약어(Domain Prefix)**:
단축 링크 경로의 앞부분. 원본 URL의 호스트명에서 TLD를 제외한 메인 라벨을 자동 추출하는 것이 기본값이며(`op.gg` → `op`), 사용자가 직접 지정할 수도 있다. 같은 원본 사이트에서 온 링크들을 구분하는 용도.
_Avoid_: 슬러그, 프리픽스

**별칭(Alias)**:
단축 링크 경로의 뒷부분. 시스템이 자동 생성하지 않고 사용자가 직접 입력한다.
_Avoid_: 커스텀 코드, 슬러그
