# 작업지침

- 한국어 사용
- 짧고 간결하게 말해줘
- 일반적인 작업 흐름 : /grill-with-docs, /to-spec, /to-tickets, /implement, /diagnosing-bugs(필요하면 사용), /improve-codebase-architure(필요하면 사용)
- 최대한 mattpocock 스킬들을 활용

## Agent skills

### Issue tracker

이슈와 스펙은 `.scratch/<feature-slug>/` 아래 로컬 마크다운 파일로 관리합니다. See `docs/agents/issue-tracker.md`.

### Triage labels

기본 5가지 표준 라벨 그대로 사용 (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

single-context 레이아웃 (`CONTEXT.md` + `docs/adr/`). See `docs/agents/domain.md`.