# 단축 링크는 생성 후 불변 — 별칭 변경은 추가/삭제로 처리

단축 링크의 원본 URL과 별칭을 생성 후에 수정할 수 있게 하면, 이미 공유된 링크가 사용자 모르게 다른 곳으로 리다이렉트되거나 갑자기 깨질 위험이 있다. 이를 막기 위해 단축 링크는 생성 후 불변으로 하고, 별칭을 바꾸고 싶으면 새 단축 링크를 추가하고 기존 것은 별도로 삭제하는 방식을 택했다. 그 결과 하나의 원본 URL에 여러 별칭이 동시에 존재할 수 있으며, 각 별칭(단축 링크)은 독립적으로 삭제된다.
